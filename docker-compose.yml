version: '2'
services:

  postgres:
    image: postgres:11-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: "vertx-in-action"
    volumes:
      - ./init/postgres:/docker-entrypoint-initdb.d/
